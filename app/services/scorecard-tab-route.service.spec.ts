/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { ScorecardTabRouteService } from './scorecard-tab-route.service';

describe('Service: ScorecardTabRoute', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ScorecardTabRouteService]
    });
  });

  it('should ...', inject([ScorecardTabRouteService], (service: ScorecardTabRouteService) => {
    expect(service).toBeTruthy();
  }));
});
